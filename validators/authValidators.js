const validator = require('../validator')

registerValidator = {
    email: {
        type: 'string'
    },
    password: {
        type: 'string'
    },
}

module.exports.registerValidator = registerValidator